# dev mode dockerfile - next.js hot reload
FROM node:24-alpine

WORKDIR /app

RUN npm install -g pnpm

COPY package.json pnpm-lock.yaml ./
RUN pnpm install

COPY . .

EXPOSE 3000

# next dev server with hot reload
CMD ["pnpm", "run", "dev"]
